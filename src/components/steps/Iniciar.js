import { Link } from "react-router-dom"

function Iniciar() {
   const id = {
    
   }
    return (
        <div>
            <button onClick={() => this.handleClick(id)} />
        </div>
    )
}
export default Iniciar